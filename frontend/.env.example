# Server-side origin used only by the Next.js BFF. Do not use a NEXT_PUBLIC_ prefix.
JUNTLY_API_ORIGIN=http://127.0.0.1:8080

# Clerk values belong only in ignored frontend/.env.local.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
