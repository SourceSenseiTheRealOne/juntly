import{createBookingReview}from"@/shared/api/generated";import{authorizedHeaders,correlated,exact,fail,requestID,requestIDHeader,sessionToken,unavailable,uuid}from"@/features/messaging/protected-bff";import{validReview}from"@/features/reviews/review-bff";export const runtime="nodejs";export async function POST(request:Request):Promise<Response>{const id=requestID(request.headers),token=await sessionToken();if(!token)return fail("UNAUTHORIZED","Unauthorized",401,id);let v:unknown;try{v=JSON.parse(await request.text())}catch{return fail("INVALID_REQUEST","Invalid request",400,id)};if(!exact(v,["bookingId","rating","body"])||!uuid(v.bookingId)||!Number.isInteger(v.rating)||Number(v.rating)<1||Number(v.rating)>5||typeof v.body!=="string")return fail("INVALID_REQUEST","Invalid request",400,id);const baseUrl=process.env.JUNTLY_API_ORIGIN;if(!baseUrl)return unavailable(id);try{const up=await createBookingReview({baseUrl,body:{bookingId:v.bookingId,rating:Number(v.rating),body:v.body},headers:authorizedHeaders(token,id)});if(up.error||up.response?.status!==201||!correlated(up.response,id)||!validReview(up.data))return unavailable(id);return Response.json(up.data,{status:201,headers:{[requestIDHeader]:id}})}catch{return unavailable(id)}}
