import { createBooking, listMyBookings, type CreateBooking } from "@/shared/api/generated";
import { authorizedHeaders, correlated, dateTime, exact, fail, requestID, requestIDHeader, sessionToken, unavailable, uuid } from "@/features/messaging/protected-bff";
import { validBooking, validBookings } from "@/features/bookings/booking-bff";
export const runtime="nodejs";
export async function GET(request:Request):Promise<Response>{const id=requestID(request.headers),token=await sessionToken();if(!token)return fail("UNAUTHORIZED","Unauthorized",401,id);const baseUrl=process.env.JUNTLY_API_ORIGIN;if(!baseUrl)return unavailable(id);try{const up=await listMyBookings({baseUrl,headers:authorizedHeaders(token,id)});if(up.error||!up.response?.ok||!correlated(up.response,id)||!validBookings(up.data))return unavailable(id);return Response.json(up.data,{status:200,headers:{[requestIDHeader]:id}})}catch{return unavailable(id)}}
export async function POST(request:Request):Promise<Response>{const id=requestID(request.headers),token=await sessionToken();if(!token)return fail("UNAUTHORIZED","Unauthorized",401,id);const body=await readCreate(request);if(!body)return fail("INVALID_REQUEST","Invalid request",400,id);const baseUrl=process.env.JUNTLY_API_ORIGIN;if(!baseUrl)return unavailable(id);try{const up=await createBooking({baseUrl,body,headers:authorizedHeaders(token,id)});if(up.error||up.response?.status!==201||!correlated(up.response,id)||!validBooking(up.data))return unavailable(id);return Response.json(up.data,{status:201,headers:{[requestIDHeader]:id}})}catch{return unavailable(id)}}
async function readCreate(request:Request):Promise<CreateBooking|null>{let value:unknown;try{value=JSON.parse(await request.text())}catch{return null};if(value===null||typeof value!=="object"||Array.isArray(value))return null;const v=value as Record<string,unknown>,allowed=["sourceType","sourceId","providerId","idempotencyKey","scheduledAt","privateLocation","agreedPriceMinor"];if(!exact(v,Object.keys(v).filter(k=>allowed.includes(k)))||typeof v.sourceType!=="string"||!["proposal","listing","direct"].includes(v.sourceType)||typeof v.idempotencyKey!=="string"||!dateTime(v.scheduledAt)||typeof v.privateLocation!=="string"||("sourceId" in v&&v.sourceId!==null&&!uuid(v.sourceId))||("providerId" in v&&v.providerId!==null&&!uuid(v.providerId))||("agreedPriceMinor" in v&&v.agreedPriceMinor!==null&&(!Number.isInteger(v.agreedPriceMinor)||Number(v.agreedPriceMinor)<=0)))return null;return{sourceType:v.sourceType as CreateBooking["sourceType"],idempotencyKey:v.idempotencyKey,scheduledAt:v.scheduledAt,privateLocation:v.privateLocation,...("sourceId" in v?{sourceId:v.sourceId as string|null}:{}),...("providerId" in v?{providerId:v.providerId as string|null}:{}),...("agreedPriceMinor" in v?{agreedPriceMinor:v.agreedPriceMinor as number|null}:{})}}
