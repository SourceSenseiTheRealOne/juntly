import { replaceConversationBlock } from "@/shared/api/generated";
import { authorizedHeaders, correlated, exact, fail, requestID, requestIDHeader, sessionToken, unavailable, uuid } from "@/features/messaging/protected-bff";
export const runtime="nodejs";type RouteContext={params:Promise<{conversationId:string}>};
export async function PUT(request:Request,context:RouteContext):Promise<Response>{const id=requestID(request.headers),token=await sessionToken(),{conversationId}=await context.params;if(!token)return fail("UNAUTHORIZED","Unauthorized",401,id);let value:unknown;try{value=JSON.parse(await request.text())}catch{return fail("INVALID_REQUEST","Invalid request",400,id)};if(!uuid(conversationId)||!exact(value,["blocked"])||typeof value.blocked!=="boolean")return fail("INVALID_REQUEST","Invalid request",400,id);const baseUrl=process.env.JUNTLY_API_ORIGIN;if(!baseUrl)return unavailable(id);try{const upstream=await replaceConversationBlock({baseUrl,path:{conversationId},body:{blocked:value.blocked},headers:authorizedHeaders(token,id)});if(upstream.error||!upstream.response?.ok||!correlated(upstream.response,id))return unavailable(id);return Response.json(upstream.data,{status:200,headers:{[requestIDHeader]:id}})}catch{return unavailable(id)}}
