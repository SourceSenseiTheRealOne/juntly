import type{AdministrationDashboard}from"@/shared/api/generated";import{dateTime,exact,uuid}from"@/features/messaging/protected-bff";export function validDashboard(v:unknown):v is AdministrationDashboard{return exact(v,["metrics","queue"])&&exact(v.metrics,["users","providers","activeListings","completedBookings","publishedReviews","openReports"])&&Object.values(v.metrics).every(Number.isInteger)&&exact(v.queue,["reports","reviews"])&&Array.isArray(v.queue.reports)&&v.queue.reports.length<=100&&v.queue.reports.every(r=>exact(r,["id","conversationId","reason","createdAt"])&&uuid(r.id)&&uuid(r.conversationId)&&typeof r.reason==="string"&&dateTime(r.createdAt))&&Array.isArray(v.queue.reviews)&&v.queue.reviews.length<=100&&v.queue.reviews.every(r=>exact(r,["id","rating","body","state","createdAt"])&&uuid(r.id)&&Number.isInteger(r.rating)&&typeof r.body==="string"&&["published","hidden"].includes(String(r.state))&&dateTime(r.createdAt))}
